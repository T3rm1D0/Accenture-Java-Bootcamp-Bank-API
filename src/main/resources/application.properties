# PostgreSQL Database Connection
spring.datasource.url=jdbc:postgresql://dpg-cubtjh8gph6c739imhg-a.oregon-postgres.render.com:5432/bank_o2ig
spring.datasource.username=bank_o2ig_user
spring.datasource.password=2DSpk4iLSA1BPSObw5mWIYKhlPYYUJnl
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false  # Disable open-in-view to avoid performance issues

# Connection Pool Settings (HikariCP)
spring.datasource.hikari.connection-timeout=30000  # 30 seconds
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=600000  # 10 minutes
spring.datasource.hikari.max-lifetime=1800000  # 30 minutes

# Logging (optional)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
